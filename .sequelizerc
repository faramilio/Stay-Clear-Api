const { resolve } = require ('path')  //resolve caminhos do windows, linux e mac

//configurações do sequelize

 module.exports = {
     config: resolver(__dirname, 'src', 'config','database.js'),
     'models-path': resolver(__dirname, 'src', 'app', 'models')
 }